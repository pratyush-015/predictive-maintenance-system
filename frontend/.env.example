# Copy to `.env` for local development against a backend running on
# localhost:8000. For docker-compose deployment, this is overridden via the
# VITE_API_BASE_URL build arg (set to empty string so the app uses relative
# paths through the nginx reverse proxy — see ../docker-compose.yml).
VITE_API_BASE_URL=http://localhost:8000
